{
  "name": "chessboard-clock",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "moment": "^2.29.2",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "3.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "surge -d https://chessboard-clock.surge.sh -p ./build",
    "release": "yarn build && yarn deploy"
  },
  "standard": {
    "globals": [
      "test",
      "describe",
      "expect"
    ]
  },
  "devDependencies": {
    "surge": "^0.21.3"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
